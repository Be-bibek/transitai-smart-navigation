name: liquid_glass_test_app
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_webrtc: ^1.3.0
  web_socket_channel: ^3.0.1
  permission_handler: ^11.3.1
  mobile_scanner: ^7.2.0
  flutter_custom_tabs: ^2.0.0+1
  flutter_bloc: ^9.0.0
  equatable: ^2.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true